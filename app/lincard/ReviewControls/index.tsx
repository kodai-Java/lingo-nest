import React, { useCallback } from 'react'

type ReviewControlsProps = {}

/**
 * 全カード終了後に「覚えてないカードだけ復習」ボタンを表示
 */
export const ReviewControls: React.FC<ReviewControlsProps> = ({}) => {
  return <></>
}
